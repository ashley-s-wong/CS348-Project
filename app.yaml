runtime: python311

entrypoint: gunicorn -b :$PORT app:app

instance_class: F1

env_variables:
  FLASK_ENV: production
  GOOGLE_CLOUD_PROJECT: cs348-final-project-458619

automatic_scaling:
  target_cpu_utilization: 0.65
  min_instances: 1
  max_instances: 2
